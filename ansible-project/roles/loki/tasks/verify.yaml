---
- name: Verify /ready endpoint responds
  ansible.builtin.uri:
    url: "http://localhost:{{ loki_http_listen_port }}/ready"
    method: GET
    status_code: 200
  retries: 5
  delay: 5
  register: loki_health
  until: loki_health.status == 200
  when: not ansible_check_mode
